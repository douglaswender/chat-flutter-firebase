import 'package:go_router/go_router.dart';

class AppRouter {
  static GoRouter get router => GoRouter(
        routes: <GoRoute>[
          GoRoute(
            path: '/',
            builder: (context, state) => ,
          ),
        ],
      );
}
